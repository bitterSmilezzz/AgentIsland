# 已核准的 git 历史豁免（scripts/scan-secrets.sh --history 读这份）
#
# 历史不能靠「删掉工作区文件」脱敏：旧版本还在 blob 里。所以这里的每一条都是
# 「明知在历史中存在、但判定为不可清除或不该清除」的记录，必须带理由。
#
# 两种粒度：
#   sha:<40位blob号>|<理由>       精确到某个对象，默认用这种
#   path:<路径前缀>|<理由>        整棵子树放行——只在「该目录按定义只放测试夹具」时用，
#                                代价是真凭据若提交在这个前缀下，历史这一关会放过它
#                                （工作区那一关仍然会拦，见 secrets-baseline.txt）
#
# 加条目的门槛：先确认命中的是假值，再写下为什么不需要改写历史。

# 测试夹具里的假 token（形如 ?access_token=<20位乱码>），掩码逻辑的对照组；
# 这个文件每改一次就多一个 blob，逐个备案只会让清单长到没人读
path:Tests/|RemoteNotifyTests 的假 token 夹具，非真实凭据

# docs/research/qoder-monitoring.md 的旧版本粘了一条本机绝对路径（含开发者账号名）。
# 当前版本已改为占位路径；改写公开历史要 force push 已发布的 118 个 tag，
# 代价大于收益 —— 该 blob 只有路径字符串，不含凭据
sha:eaad946e6717ff487c474e8e5cd19ffc0382b570|旧版调研文档里的本机路径，已在 v0.0.119 脱敏
