# v0.0.164 独立复核 · DSH · 2026-09-27 01:06

## 范围与结论

复核「内置档案数」这一族数字：README、CONTEXT、工作台若干文档与一处 Rust 注释。
结论：**26 是错的，实际 25**；错因可定位到一条取证命令本身。本轮把 6 个文件、
25 处改齐，未改动任何 agent 档案、`builtin` 数组内容或运行时行为。Swift 侧零改动。

## 可核验的行为证据

- 运行时求值（把编译好的 `AgentIslandCore.o` 链进一次性探针，不靠读源码数行）：
  `builtin.count = 25`、`distinct ids = 25`、`sessionDirs` 非空 25、`tokenRoots` 非空 5
  —— `dim` / `claude` / `codex` / `workbuddy` / `workbuddy-ai`。非 token 的 20 家与
  21 号文档原列名单逐个对上（原名单多算了 `cli-*` 一条动态族）。
- 错因定位：`docs/workbench/23` 原写 `grep -c 'AgentProfile('` = 26。该命令统计整文件，
  把 `AgentRegistry.swift:415` 的 `discoverCLIProfiles()` 里**动态构造**的那条也算进去；
  `builtin` 数组本体是 `:29-400` 的 25 条。已在文中写明这个坑，并把取证口径换成
  「只认 `builtin` 数组」。
- 旁证：`site/index.html` 两处本来就写 25（「内置工具档案 25 个」），与本轮结论一致；
  错的是仓内文档而非站点。
- 同批过期数字逐个实测（`wc -l`）：`AgentRegistry.swift` 604、`registry.rs` 265、
  `engine.rs` 678 已改正；`ActivityEngine.swift` 1492、`ProcessMonitor.swift` 700、
  `procmon.rs` 129 经复核本来就对，未动。CLI 子命令按 `main.swift` 实点为 12
  （`version`/`help` 是旗标不是子命令），21 号原写的 13 已改。
- `Rust 缺 14 个 id` 经集合差复核**成立**（Swift 25、Rust 12、Rust 独有 `vscode`），未动。
- 门禁：`cargo test --locked` 62 通过 0 失败；Swift 测试 549 通过 0 失败，其中
  「结构: README 不许长成更新日志」「哨兵: CHANGELOG/build-app.sh/README 三处版本一致」
  「版本单一来源」全绿；`git diff --check` 通过。

## 边界与后续风险

- 本轮只改数字与取证口径，**未重新核实各档案的路径是否真在本机存在**——21 号那条
  「未逐家验证路径真实存在」的保留意见仍然成立，且现在覆盖面从「26 家」缩到 25 家。
- 未重测的计量口径：`site/` 与 `docs/research/` 里的字符数（如 17 号的「23946 字符」）、
  工作台 23 号表内其他行数。这些与档案数不是同一类断言，留待单独核查，本轮已在
  CHANGELOG 里明说没做。
- 档案数是会随新增档案变动的活数字。23 号现在写清了口径，但没有守护测试拦「文档数字
  与 `builtin.count` 漂移」——除 Rust 侧那条 `>= 12` 外，两边分叉仍靠人盯。
  若要根治，下一个该做的是把这条口径做成可执行断言。
