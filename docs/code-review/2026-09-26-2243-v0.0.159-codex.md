# v0.0.159 独立复核 · Codex · 2026-09-26 22:43

## 范围与结论

复核本轮 Rust 五态、Codex/Cline 会话解析、合成 fixture、脱敏守护、发版入口及同步文档。
结论：本轮变更可发布，未发现需阻止提交的未解决问题。M1 仍是**部分完成**：
`provider.rs` 不存在，其原子写与掩码守护尚无对象；不能把这轮的 44 条 Rust 测试
当作 Provider 或双形态迁移已通过的证据。

## 可核验的行为证据

- `cargo test --locked --manifest-path app/src-tauri/Cargo.toml`：44 通过，0 失败。
  五态回放直达生产 `decide_level`；最初 10 秒后的采样仍返回 `Working`，
  修复后回 `Idle`。长任务更新锚点、时钟回拨、进程退出重启、CPU 阈值、
  文件写入窗口及事件指纹去重均由合成时间守住。
- 合成会话文件经 `session::probe` 的真实文件入口读取：Claude 工具/确认/结束，
  Codex 调用 ID 配对、请求回复、明确 `task_complete`，Cline 命令/确认/输出/完成，
  以及空文件、坏行、未知方言。红测曾显示 Codex 将普通消息判为完成、结果记录
  无法收口调用、同命令重复调用共用指纹；相应测试转绿。
- `scripts/test-scan-secrets.sh`：7 通过，0 失败。每条负例核对预期故障原因
  和无最终通过标记；杀死扫描进程的用例核对退出码 137。
  守护还揭出 shell 将全角符号粘到 `$rc`/`$BASELINE` 后解析为错误变量名，
  以及缺基线时诊断被提前失败吞掉；均已修复。
- `cargo build --locked` 与 `scripts/scan-secrets.sh --release` 通过。
  发布脚本在任何提交前再执行扫描、脱敏守护与 Rust 测试，然后进入 Swift 测试和打包。

## 范围边界

Rust `app/` 尚未进入正式下载包；当前下载包与重启对象仍是 Swift 原生端。
合成 fixture 不声称覆盖所有上游协议版本或真实安装环境，跨屏及 Windows/Linux 真机验收仍需另做。
文档已将 v0.0.158 的调查数字标为时点快照，修正了工作区与五态排序的过期结论。
