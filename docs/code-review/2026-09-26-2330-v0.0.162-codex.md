# v0.0.162 独立复核 · Codex · 2026-09-26 23:30

## 范围与结论

复核 ZCode 本机目录与匿名结构取证、Swift/Rust 档案入口、回归测试、Trae/Windsurf
未核实项和文档口径。未发现阻止发布的问题。修复只针对已证实的 ZCode 任务目录；
另外两款没有本机实样，未据产品名称猜测目录。

## 可核验的行为证据

- 本机 `~/.zcode/cli/rollout` 有 1 个 JSONL、89 条合法 `model_io` 记录；每条都有
  `response`、`completedAt`、`requestId` 与正数净用量。`~/.zcode/cli/log` 的 2221 条
  记录没有 `response`，且该文件更新更晚。`~/.zcode/v2/checkpoints` 当前不存在；
  只读任务库存在。取证方法和边界见 `docs/research/2026-09-26-agent-session-paths.md`。
- Swift 保留 checkpoints 兼容路径并加入 rollout；Rust 去掉 log，保留 rollout 作为
  会话和 token 来源。两端新增的档案测试各守住相应目录选择。
- `cargo test --locked --manifest-path app/src-tauri/Cargo.toml`：58 通过，0 失败。
  使用项目脚本指定的 macOS 26.5 SDK 构建后，Swift 自建 runner：549 通过，0 失败。
  `git diff --check` 通过。

## 边界与后续风险

一台机器的当前安装不能证明旧版 ZCode 不用 checkpoints，因此 Swift 保留它。
仅从字段可见 CLI log 不属于 `probe_zcode` 的模型响应结构；关于空闲更新可能
误触发 `Working` 是基于现有 FileMonitor 规则的推断，没有持续空闲采样证据。
Trae/Windsurf 的路径仍待安装环境实测。ZCode 两端状态/动作解析深度仍不同；
M3 缺失模块与侧边栏没有在本轮完成。
