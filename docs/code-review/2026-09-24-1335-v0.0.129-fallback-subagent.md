# Code Review — AgentIsland

- 审查时间：2026-09-24 13:54
- 覆盖：2/2 个文件（本轮声明的增量 = `git diff v0.0.128` 的两个测试文件；审查期间工作树又出现 3 个版本同步文件，见「备注」）
- 审查路径：**退化路径**。外部评审 CLI 全部不可用——`dsh` 对 provider `dimagent-oauth` 返回 `NO_ADAPTER`，`claude` / `codex` / `opencode` / `gemini` 均未安装（本机 `command -v` 实测）。故本轮由**非作者的独立 agent** 审，方法：只读精读 + 把 `checklistViolations(_:)` 逐字搬到 /tmp 单独编译成可执行体，用合成文档与真实文档变异体跑判据，再用 `git show v0.0.128:` 的原码跑同一变异做对照。
- 结论：扩围是真的（循环 + 逐文档违规清单 + 「一份都没校验即失败」的兜底都成立，变异 (a)(b)(c)(e) 逐条实测转红），断言**不再靠崩溃表达意见**（表头缺列按列名取 nil 而非下标越界），① 的行形下界未被削弱（6 种写错的编号格实测全红）。但守卫仍有**七条报绿而约定已破**的口子，其中三条可以一句话绕过：把想写的话包进 `<!-- 状态词表 -->`（声明块不校验内容，实测全绿）、把清单的「待核实」列改名（整份文档进 skipped 桶，实测全绿）、把状态判定从编号引用的下一行挪到下一段（隔一个空行即绕过，实测全绿）。另有两条「清单还欠着什么」的硬信息根本没被守：**「待核实」列只查列名不查内容**（八行描述全空仍绿）、**编号可重复**（`tableIDs` 是 Set，重复静默折叠）。崩溃面本轮未找到：7 000 份对抗输入 0 陷阱，全量 451 条 0 失败。注释 ② 的因果叙述与本轮 CHANGELOG 及它自己 :458 的新注释互相矛盾（我用上一版原码跑同一变异：它压根到不了 `cells[1]`）。

## 变更概览

| 文件 | 类型 | 变更量 | 实质 |
|---|---|---|---|
| `Tests/AgentIslandTestsRunner/DebtPayoffTests.swift` | test | +188 / −105 | 断言体（`:292-327`）从「一份文档 + 内联 expect」改成「`docs/research/` 全量 + 逐文档违规清单」；校验体搬到文件作用域 `checklistViolations(_:)`（`:371-526`）；注释从三条失效面扩到五条（`:294-311`） |
| `Tests/AgentIslandTestsRunner/TestKit.swift` | test | +34 | 新增 `SourceTree.markdownTexts(under:atLeast:)`（`:257-289`）：存在性 → 枚举 → 数量下限三段式失败，返回 `(相对仓库根路径, 正文)` |

形状上的三个真实加强（与 v0.0.128 对照）：

1. **一份文档错三处一次报全**。旧版第一个 `expect` 就抛，后面的文档连看都没看；新版 `checklistViolations` 返回 `[String]`，断言侧累加。
2. **合法区从「按行内容」改成「按行号」**（`:478-482`）。旧版 `nonTable` 用「与任一数据行文本相等」豁免，跨文档/同文档同形表行会互相豁免。
3. **声明块闭合必须配对**（`:466-476`）。旧版没有闭合检查，删掉 `<!-- /状态词表 -->` 会让其后全部正文静默变成免检区——实测新版这条会红 4 项。

## 本轮变更的验收（逐条对照上面「三项」）

### 1) 扩围到整个 `docs/research/`（闭 `.scratch/audit-42-research-doc-coverage.md`）——**机制成立，今天的树上是 0 增量覆盖**

- 实测：`markdownTexts(under: "docs/research", atLeast: 2)` 扫到 3 份，触发 `text.contains("待核实")` 的只有 `agent-lifecycle-hooks.md` 1 份，另 2 份（`qoder-monitoring.md`、`remote-notifications.md`）**当前 0 次命中**（`grep -c` 实测）。所以校验集合与 v0.0.128 那份硬编码文件**完全相同**——扩围是预防性的，这点 CHANGELOG 自己写了，判定为符合 issue 的验收（验收要求的正是「另一份文档加了清单会红」，实测红，见下）。
- **触发条件放进了什么**：任何位置出现「待核实」四连字，包括代码块内、引用块内、以及「本文档暂无待核实项」这种**声明没有欠项**的句子（实测 `c9`：**红**，报「没有以 `| 编号 |` 开头的一行」）。
- **放过了什么**：任何不含这四字的文档。等价说法一抓一把——「未核实」「待复核」「尚未验证」「存疑」「未决项」——而且清单表头那一格本身就叫「待核实」，也就是说**触发词与表格列名是同一个词**。实测 `c10`：一张完整存在、状态写着「随便写点什么」、复核方式写着「没有动作」的清单，只把列名从「待核实」改成「未决项」，就整份进 skipped 桶，一条违规都不产生（全绿）。
- **激励方向是反的**：`c9` 那种红，最省事的修法是**把触发词删掉**——而那正是让守卫对该文档永久失效的动作。
- **该不该逼每份文档建表**：不该，issue 与 v0.0.127 review 都写明了，本轮选择正确。但下面这条要指出：CHANGELOG 把「触发条件做成含表格即校验」列为**被否决的写法**，理由是「等于放弃『散文写未完成项』这一整类守卫」——这个二选一是不成立的。`contains("待核实") || lines.contains { $0.hasPrefix("| 编号 |") }` 是当前条件的**严格超集**，散文类一条不少，只补上「有表却没被看」这一类。见 P1-6。
- **兜底有效**：把三处「待核实」全删（实测 `mut/e`）→ `checked` 为空 → `expectTrue(!checked.isEmpty, …)` 红。目录被改名（实测 `fileExists(isDirectory:)` 返回 false）→ 抛「不存在或不是目录」，报的就是那一句。

### 2) 修上一轮 P2-1（`tableIDs` 读 `cells[1]`）——**代码改了，但注释 ② 的因果是错的，而这一改是不可观察的**

把 v0.0.128 的断言体逐字搬进 /tmp 单独编译，喂同一份变异（在编号列前插入 `| 序 |`）：

- 实测输出：`红 → 清单表头必须是以「| 编号 |」开头的一行`。**旧代码根本走不到 `cells[1]`**——定位条件 `hasPrefix("| 编号 |")` 就要求「编号」是第一列，于是 `cells[1]` 与 `idx("编号")` 在任何能被定位到的表上恒等。
- 也就是说本轮这一处修复**行为等价**，而它换来的价值只剩「同一段代码里两种定位方式并存」这条一致性。CHANGELOG 已经把这个因果改对了（「实测不发生」），:458-460 的新注释也写对了；但 **:299-302 的注释 ② 仍然在讲那个不会发生的误诊**。见 P1-7。
- 真正该写进注释的约束反而没写：**「编号」列必须是第一列，否则这张表对断言不存在**。

### 3) 修上一轮 P2-2（同义说法禁令是行级的）——**「下一行」修好了，「下一段」没修**

- 实测 (b)：在真实文档 `⇒ **V3**。` 后面紧接一续行写「这个说法本轮没逐字复核。」→ **红**，消息点名行号与命中词（`第 107 行在同一段里换了个说法又下一次判定`）。加强是真的：同一输入拿 v0.0.128 的原码跑，绿。
- 实测 (反例)：中间加一个空行 → **全绿**（`c4-blankline`）。段边界是空行，而 `flush` 的前置又是「这一段里必须有合法编号」，于是「换个段落写判定」和「把编号写成非粗体」两种写法都在盲区里。当前文档 :270-272 那段元叙述正是踩在前置条件上（`⇒ **V#**` 里的 `V#` 不是合法编号）才没被误伤——同一格式位置也能装真正的第二出口。见 P1-5。

### 注释 ①–⑤ 逐条对代码核实

| 条 | 注释声称 | 实测判定 |
|---|---|---|
| ① 行形下界 | 每个数据行的编号格必须严格 `**V<数字>[单个字母]**`，`v4`／裸 `V9`／少空格都红 | **未被削弱**（`tokenID`/`boldID` 逐字搬迁）。实测 6 种坏写法全红：`v1`、`V9`、`**V 1**`、`**V1aa**`、`**V1 **`、`**V1**（新）`。但作用域只覆盖「从 `headerAt+2` 起连续以 `\|` 开头的行」——见 ④ 与 P1-4 |
| ② 列索引全部从表头推 | 插一列会让旧代码误诊成「编号没成对」 | **因果为假**（实测旧代码先红在定位条件）；改动本身**不可观察**。见 P1-7 |
| ③ 合法区 = 表格数据行 ∪ 显式声明块 | 放行靠声明 | 「按行号」相对旧版是加强；但**声明块不校验任何内容**（实测 `y1` 把三种状态词 + 四个同义说法包进块里 → 全绿；`y2` 块内空无一字 → 全绿），于是它是一个无界豁免键。且 `tableRegion` 把 `headerAt+1` 那一行**无论写什么都**划进免检区（P1-4） |
| ④ 禁词扫描单位是段 | 挪到编号引用的下一行就看不见 | 「下一行」已堵（实测红）；「下一段」未堵（实测绿）。且名单里的「该页未列／文档未提」是**发现**用词，与注释「别处正常中文不误伤」冲突（实测 `c11` 红） |
| ⑤ 覆盖面是整个 `docs/research/` | 不再是一份文件 | 结构上成立；今天的校验集与 v0.0.128 **同一份文件**（1/3 命中）。兜底 `!checked.isEmpty` 有效（实测 `mut/e` 红） |

### 变异清单（作者声明的 5 条 + 本轮补的 3 条）实测结果

| 变异 | 声明 | 实测 |
|---|---|---|
| (a) `qoder-monitoring.md` 加 `## 待核实清单` + 散文未完成项 | 应红 | **红** ✓（报「没有以 `\| 编号 \|` 开头的一行」；`checked=2／skipped=1`） |
| (b) 判定挪到 `⇒ **V2**` 的下一行 | 应红 | **红** ✓（点行号；旧码同输入绿 → 真加强） |
| (c) 删掉 `<!-- /状态词表 -->` | 应红 | **红** ✓（4 条：无配对 + 声明块里三个状态词各一条） |
| (d) 改名 `docs/research` 目录 | 应红 | **红** ✓，且报的是「不存在或不是目录」而不是「只扫到 0 个 .md」 |
| (e) 全部文档去掉「待核实」 | 应红 | **红** ✓（`checked=0`） |
| (f) 删掉清单的对齐行 `\| :--- \|` | 未声明 | 红，但**误诊**成「正文引用了清单里没有的编号：["V1"]」；换个形状就**全绿**（P1-4） |
| (g) 复制一行、编号不改成重复的 `**V6**` | 未声明 | **全绿** ✗（P1-3） |
| (h) 八行的「待核实」描述格清空 | 未声明 | **全绿** ✗（P1-2） |

## 发现的问题

### P0 — 阻断

无。崩溃面是本轮重点排查对象，结论为**没有找到能让 runner 崩掉的输入**：把 `checklistViolations` 逐字编译成可执行体后，4 000 份随机拼装文档 + 3 000 份真实文档随机变异体全部正常返回（0 非零退出）；`(o+1)..<c` 被 `c > o` 保住、`Set(headerAt...max(headerAt, rowCursor-1))` 由 `rowCursor` 的构造保证下界、`column(_:_:)`（`:415-418`）取代了旧的裸下标、函数体内无 force unwrap、无 `try!`、无 `precondition`。全量套件 451 通过 / 0 失败。

### P1 — 重要

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:466-482`** — 声明块 `<!-- 状态词表 -->` 是一把**不校验内容的无界豁免键**，注释 ③ 却把它写成「显式声明的**定义块**」。
  - 触发：把任意一段散文包进这两个标记（`y1-exempt-clean`：块内一行写了「本轮没逐字复核，正文未出现相关事件名，属二手转述」，另一行把三种状态词当普通词用）；或把块内文字删空只留两个标记（`y2-empty-defblock`）。
  - 症状→后果：实测两种都**全绿**。`declared` 只按行号收进 `legal`（`:471`、`:481-482`），既不看块内是什么、也不看块多长（唯一要求是「有一行整体等于开标记，且有一行整体等于闭标记在其后」）。于是「状态只写在清单表的状态列」这条约定**有一个纯语法的后门**：谁想把第二出口写进正文，把它包进标记即可，而消息里那三个状态词本来就是文档教出来的写法。`legal` 同时还把 `body`（`:486`）里这些行摘掉，块越大、编号配对越盲。
  - 建议修法：在 `:469-476` 之后加三行判据——块内必须**逐字出现**三个带反引号的状态名（`` `未找到出处` `` 这种形式，逐个比对）（`let body = declared.map { lines[$0] }.joined(separator: "\n")`，`for w in allowed where !body.contains("`\(w)`")` → `bad.append("声明块没有定义「\(w)」——它只许定义词汇，不是豁免区")`）；块长超过 4 行即报「声明块超长」；块内出现 `paraphrase` 任一命中即报。约 8 行。
  - 能否自动修：**否**（代码是机械的，但「什么算定义块」是口径决策，要先在 `docs/research/agent-lifecycle-hooks.md:250-253` 上验一遍不误伤）。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:409-411, 435-457`** — 「待核实」列**只查列名存在，从不查格子里有没有内容**，而它恰恰是「哪条还没核」这句话本身。
  - 触发：`x2-emptydesc`——把真实文档八行的第二格全部清空（`| **V1** |  | 未找到出处 | … |`），编号／状态／复核方式三格保持合规。
  - 症状→后果：实测**全绿**。逐行校验覆盖了 `编号`／`状态`／`复核方式` 三格，第四格 `待核实` 只在 `:409` 的表头名单里出现一次。清单可以八行全空而断言宣布合规——这正是 v0.0.125/126 付过学费的形态（「不看全文就不知道这条到底欠着什么」），而守卫的名字就叫「可判定」。
  - 建议修法：在 `:444` 前后加两行，复用已有判具：`let desc = column(row.cells, "待核实") ?? ""; if placeholders.contains(desc) || desc.count < 6 { bad.append("\(where_)「待核实」格没有内容（\(desc.prefix(10))）——这一条等于没写") }`。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:462, 487-491`** — 编号唯一性没有被守：`tableIDs` 是 `Set`，两行同编号静默折叠。
  - 触发：`c2-dup` / `mut/g`——复制表里最后一行、编号照抄（真实文档里把 `**V6**` 那行原样再插一行），正文只引用一次 `**V6**`。
  - 症状→后果：实测**全绿**。`dataRows` 有 9 行、`tableIDs` 只有 8 个：`noRow`/`orphan` 两个方向都看不出少了一条，「未完成项共 N 条」这个数被读成 8 而实际欠 9 条。这套约定存在的理由就是「不用通读全文也能数清还欠几条」，而计数是它唯一没落实的输出。
  - 建议修法：`:462` 改成先收带重复的列表 `let rowIDs = dataRows.map { column($0.cells, "编号") ?? "" }`，`let tableIDs = Set(rowIDs.compactMap(boldID))`，再加 `let dup = Set(rowIDs.filter { id in rowIDs.firstIndex(of: id) != rowIDs.lastIndex(of: id) })`；`dup` 非空即 `bad.append("编号重复：\(dup.sorted())——\(dataRows.count) 行只有 \(tableIDs.count) 个不同编号，条目数读不出来")`。约 4 行。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:421-431, 481`** — `rowCursor = headerAt + 2` 把「表头下一行是对齐行」当成事实，**从不验证**；不成立时那条数据行既不被校验、也不被任何消息提到。
  - 触发：删掉 `| :--- | :--- | :--- | :--- |`（真实文档实测为 `mut/f`），或数据行里第一格写成 `已核实完成` 这类第四种状态词——`c1-noalign`（两条数据行、无对齐行、首行状态与复核方式都不合规）。
  - 症状→后果：`c1-noalign` 与 `c21-invisible`（首行是条**正文从未引用**的编号 `**V9**`）实测**全绿**：该行进了 `tableRegion`（`:481`，`headerAt...rowCursor-1` 无条件覆盖 `headerAt+1`）→ 被 `isLegal` 免检、又被 `body` 摘掉 → 它的编号在 `tableIDs` 和 `bodyIDs` 两边都不存在 → 一条清单项彻底隐形。当该行恰好被正文引用时（真实文档的形状）才会红，而报的是「正文引用了清单里没有的编号：["V1"]」——**把「对齐行没了」误诊成「编号没成对」**，正是本轮 :458-460 声称要消灭的那一类不诚实消息。
  - 建议修法：`:421` 前显式核对 `sep = headerAt + 1`：若 `sep < lines.count` 且 `lines[sep]` 不含 `":--"`，报「表头下一行不是 `:---` 对齐行而是 `<其前 30 字>`，数据行扫描的下标基准不成立，第一条数据行会被跳过」；并把扫描起点改成 `var rowCursor = (sep < lines.count && lines[sep].contains(":--")) ? sep + 1 : headerAt + 1`，让 `tableRegion` 只覆盖真正被解析过的行。约 5 行。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:498-523`** — 段级禁词的段边界是**空行**，且 `flush` 的前置是「这一段自己含合法编号」，两者叠加使上一轮 P2-2 那一类漏洞只上移了一层。
  - 触发：`c4-blankline`——`引用 ⇒ **V1**` 一段，**空一行**，再一段写「这一段又下一次判定：本轮没逐字复核。」。对照 `c4b-samepara`（同段下一行）红。
  - 症状→后果：实测**全绿**。含禁词的段没有 `**V<数字>**` → `guard !boldIDs(joined).isEmpty`（`:508`）直接 return，禁词名单根本不查。同一个位置把编号写成裸文本 `（见 V2）`（`c5` 实测）也一样不进判据——只是因为该编号变成 `orphan` 才红，那是另一条断言在替它兜底。真实文档 :270-272 那段元叙述正落在盲区里（`⇒ **V#**` 里 `V#` 不是合法编号 → 整段不查），说明这不是假想形状而是**当前就在用的书写位置**。
  - 建议修法：把判据从「段里有编号」换成「段属于清单小节的作用域」：先用 `let sectionStart = lines.firstIndex { $0.hasPrefix("## ") && $0.contains("待核实") }`、`sectionEnd` 取其后第一个 `## `，落在该区间的**每个非 legal、非空段**都跑禁词，不再要求段内含编号；区外维持现状（不误伤发现句）。约 10 行。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:315`** — 触发条件只有 `text.contains("待核实")` 一个出口，改名即整份脱离；而「声明没有欠项」的句子反被冤枉。
  - 触发：`c10-nomagic`——一份**有清单**的文档把列名「待核实」改成「未决项」（或把小节标题改成 `## 未决项清单` 并同步列名）。以及 `c9-nothing`——一句「本文档暂无待核实项」。
  - 症状→后果：前者实测进 `skipped`、**0 违规**（其状态格写「随便写点什么」、复核方式写「没有动作」，全都不被看）——扩围后守卫对它的覆盖率回到 0，且**新加一份研究文档时最省事的「通过」方式就是不写这个词**。后者实测红，红完之后最省事的修法同样是删掉这个词。两个方向都在教作者**绕开触发词**，而 `!checked.isEmpty` 只在「一份都不剩」时才兜得住。
  - 建议修法：`:315` 改成 `let hits = doc.text.contains("待核实") || doc.text.contains("<!-- 状态词表 -->") || doc.text.split(separator: "\n").contains { $0.hasPrefix("| 编号 |") }`——有表、有声明块标记、有触发词，任一即校验。这是当前条件的严格超集，散文类守卫一条不减，且完全不逼任何文档建表（issue 的那条约束仍然守住）。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:299-302`** — 注释 ② 描述的失效路径**不会发生**，并与同一文件 :458-460 的新注释、以及 CHANGELOG 0.0.129 的「实测**不成立**」互相矛盾。
  - 触发：读代码的人按注释找那个「在编号列前插一列 → 误诊成编号没成对」的场景；或下一轮有人据此把定位条件放宽。
  - 症状→后果：实测（`git show v0.0.128:` 原码 + 同一变异）旧版红在「清单表头必须是以「\| 编号 \|」开头的一行」，**报的是正确那句**，从未走到 `cells[1]`；因为 `hasPrefix("| 编号 |")` 已经把「编号」钉死为第一列，`cells[1] == idx("编号")` 恒成立。于是本轮这处「修复」不可观察，而注释把一个不存在的失效写成已修的第五面——注释是这个断言唯一的规格说明，写错等于规格错。**真正的约束没写下来**：编号列必须是第一列，否则整张表对断言不存在（实测 `y3`：把表头改成 `| 待核实 | 编号 | … |` → 报「没有以 `| 编号 |` 开头的一行」，读者不会知道原因是列序）。
  - 建议修法：:299-302 整段按 :458-460 的口径重写（「列索引一律从表头推；但**定位条件要求「编号」是第一列**，所以今天的 `cells[1]` 与 `idx("编号")` 恒等——按列名取是为了定位条件放宽那天不静默指错列」）；更彻底的一步是把 `:404` 的定位放宽成「以 `|` 开头且 `cells` 含「编号」的行」，让注释那句「全部从表头推」第一次变成真话。
  - 能否自动修：**是**（注释同步 + 定位条件一行；后者要先跑 `mut/f`/`y3` 两份变异确认不失去现有的响亮失败）。

### P2 — 建议

- **`Tests/AgentIslandTestsRunner/TestKit.swift:271-273`** — `guard let en = FileManager.default.enumerator(…) else` 是**不可达分支**，:264 的注释却声称「三种失败必须报不同的话」。
  - 触发：任何输入。实测 `enumerator(at:includingPropertiesForKeys:)` 对 ①不存在的目录 ②该路径是普通文件 ③空目录 ④权限 0o000 的目录 **全部返回非 nil**（作者对①的判断是对的，helper 的存在性检查因此是必要的）。
  - 症状→后果：实际只有两种话；「无法枚举 …」永不出现。真正会撞上的是 ④：一个存在但读不了的目录会报「只扫到 0 个 .md」，把读者往「文档太少」带走——正是 :264-266 声称要避免的那个误诊，只是换了个触发条件。
  - 建议修法：把 `guard let` 换成 `let en = …; if en == nil { … }` 留作保险但在 :264 注释里改口为「两种失败各有各的话」；并在数量下限那条消息里带上 `FileManager.default.isReadableFile(atPath: dir.path)`，让「读不了」与「太少」分道。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/TestKit.swift:275`** — `.md` 后缀硬编码且大小写敏感，改名即从清单上静默消失。
  - 触发：实测目录内放 `a.md`／`B.MD`／`c.markdown`／`sub/d.md`／`.hidden.md`，filter 只给 `a.md` + `sub/d.md`（递归与隐藏文件都对，`.MD` 与 `.markdown` 直接不见）。
  - 症状→后果：一份研究文档改扩展名就脱离守卫，且 `urls.count` 跟着掉——只要还剩 2 个 `.md` 就不响。叠加 P1-6 的「不写触发词」，两个互不相干的改名就能让整条守卫对某份文档永久无效。
  - 建议修法：`let ext = $0.pathExtension.lowercased(); return ext == "md" || ext == "markdown"`；并把返回的 `(relativePath, text)` 与 `git ls-files docs/research` 的数量做一次下限对照（可选）。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:326`（+`:312`）** — 违规清单截到 10 条且**不按文档分组**，全绿时又完全读不到覆盖情况。
  - 触发：实测 `a-many.md`（12 行不合规）+ `z-many.md`（1 条）共 26 条违规 → 断言消息只有前 10 条，**全部来自第一份文档**，第二份文档的违规一条都看不见；消息里也没有「共 26 条」。
  - 症状→后果：`:369-370` 那段注释立的目标是「剩下几份是合规还是没看过，读者要读得出来」，实测只做到了「同一份文档内不再只报第一条」。跨文档方向上仍然会被第一份的噪声挤满。另外 `atLeast: 2` 而实有 3 份：删掉任意一份不响。
  - 建议修法：按文档分组再截——`let byDoc = Dictionary(grouping: offenses, by: { String($0.prefix(while: { $0 != ":")) }) }`，每份取前 3 条，消息头写 `offenses.count` 与 `checked`/`skipped`；`atLeast` 改 3 或在 `:312` 注释里写明「3 份、下限 2 是故意留一格给新文档」。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:404 与 :469-470`** — 表头与声明块标记都**不容任何前导空白**，而标记匹配还从 v0.0.128 的「trim 后比较」退化成整行精确相等。
  - 触发：实测 `c19-indented-mark`（`  <!-- 状态词表 -->` 缩进两格）→ 红「词汇定义必须包在块里」；把清单塞进列表项（`  | 编号 | …`）→ 红「没有以 `| 编号 |` 开头的一行」。真实文档 :223-229 那种三格缩进的正文块就在同一份文件里。
  - 症状→后果：v0.0.128 用 `t == openMark`（trim 后）能接受缩进，本轮反而不接受——方向上是**回退**。失败是响的，但报的是「你没声明」，而真因是「你缩进了」；一张写进编号列表的清单会被判成「散文写了未完成项却没建表」，正把作者往「把表格拆回散文」的方向推。
  - 建议修法：定位统一走裁剪：`:404` → `lines.firstIndex(where: { $0.trimmingCharacters(in: .whitespaces).hasPrefix("| 编号 |") })`（并把 `:422` 的 `hasPrefix("|")` 同步裁剪），`:469-470` → 用 `lines.enumerated().first { $0.element.trimmed == openMark }?.offset`；`cells` 与状态格已经 trim 过，不会因此变松。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:409-411 → :435-457`** — 一个根因（表头少一列）会放大成 2N+1 条下游噪声。
  - 触发：实测 `miss-col`（表头删掉「复核方式」列、三行数据）→ **7 条**违规：1 条「缺列」+ 每行「是占位符（）」+ 每行「没写动作」。
  - 症状→后果：`column` 返回 nil 时 `?? ""` 让下游校验照跑，`""` 又正好在 `placeholders` 里——诊断信息把「表头少一列」说成「每一行都在滥用占位符」，而 P1-4 已经证明这种「响但不诚实」正是本轮要消灭的类别。
  - 建议修法：`:409-411` 记下缺失列名单 `var missingCols: [String] = []`；`:435` 的循环开头 `guard missingCols.isEmpty else { continue }`（根因已报，不再刷行级噪声）。约 3 行。
  - 能否自动修：**是**。

- **`Tests/AgentIslandTestsRunner/DebtPayoffTests.swift:502-503`** — 禁词名单里的「该页未列」「文档未提」「正文未出现」是**发现**措辞，与 :500 的「别处正常中文不误伤」直接冲突。
  - 触发：实测 `c11-finding`——`引用 ⇒ **V1**，该页未列相关事件名。` 单行单段 → 红。真实文档表格里第 5a 条的「待核实」格现在正写着「未找到出处（该页正文未出现 hooks）」（:263），把这句话照抄到正文的 `⇒ **V#**` 后面是很自然的动作。
  - 症状→后果：误伤面随本轮从行扩到段而**变大**（整段任一行命中即报），而名单本身混了两类词：真正的「换词重下判定」（没逐字复核／未找到正文依据／二手转述）与「陈述证据边界」（该页未列／文档未提）。混在一起的后果是可预期的——被冤枉的作者会来放宽名单，最后连真判定也不红了。
  - 建议修法：把「该页未列」「文档未提」「正文未出现」移出 `paraphrase`，改成只在**清单小节作用域内**禁（与 P1-5 的 section 判据共用），区外当作发现句放行。
  - 能否自动修：**否**（哪几个词算「第二出口」、哪几个算「发现」是口径决策）。

## 未覆盖

- 断言**能否守住在 CI/别的机器上**未测：`#filePath` 走的是编译期绝对路径（`TestKit.swift:227`），换机器需重新编译——本轮只在本机 `swift build --build-tests` 跑过。
- `docs/research/` 三份文档正文里的对外部厂商文档的引用（Qoder / Cursor / Trae / Cline 字段表）未独立核实，属「项目方已声明核过、本轮未复核」。
- 未跑 `scripts/scan-secrets.sh`（只读约束），脱敏门禁状态未验证。

## 独立实测记录

**测量（跑了什么、返回什么）**

1. `swift build --build-tests` → `Build complete! (38.97s)`；收尾再建一次 → `Build complete! (47.13s)`。
2. `.build/debug/AgentIslandTestsRunner` → `结果: 451 通过, 0 失败`（改动前后各跑一次，用例数不变，与 CHANGELOG 声明一致）。
3. `grep -c '待核实' docs/research/*.md` → `agent-lifecycle-hooks.md: 4`、`qoder-monitoring.md: 0`、`remote-notifications.md: 0`。→ 今天 `checked=1／skipped=2`。
4. **`enumerator` 语义验证**（Swift 片段，四种路径）：不存在的目录 → `enumerator is nil? false`、`yielded objects: 0`；`fileExists(isDirectory:)` → `false false`；该路径是普通文件 → `fileExists true / isDir false`；空目录 → 非 nil、0 项；权限 0o000 的目录 → 非 nil、0 项。**作者「对不存在的目录不返回 nil」的判断成立**；`guard let en` 分支不可达。
5. **`markdownTexts` 复刻验证**（把 :257-289 逐字搬到 /tmp，`repoRoot` 换成可控根，构造合成目录树）：正常 → 返回 2 项；目录改名 → 抛「不存在或不是目录…」；只剩 1 个 `.md` 且 `atLeast: 2` → 抛「只扫到 1 个 .md…」；目录内 `B.MD` + `c.markdown` + `a.md` 且 `atLeast: 1` → **`OK n=1 rels=["a.md"]`**（两份静默消失）；路径是普通文件 → 抛「不存在或不是目录…」；空目录 → 抛「只扫到 0 个 .md…」。
6. **`checklistViolations` 复刻验证**（:371-526 逐字搬出 + 一个复刻 `:314-318` 分发循环的 driver）：真实三份文档 → `checked=1 offenses=0`（与跑真套件一致，证明复刻可信）。
7. **崩溃排查**：4 000 份随机拼装文档（含 `||`、裸 `|`、`**`、`***`、emoji 编号、`| 编号 |` 重复行、空行）+ 3 000 份真实文档随机变异（删行／插行／重复行／换行）→ **非零退出 0 次**；`Set(headerAt…rowCursor-1)` 与 `(o+1)..<c` 无一次 trap。
8. **单行删除敏感度扫描**（真实文档 292 行逐行删）：删了会红的只有 **20 行**——8 处正文 `⇒ **V#**` 引用（:60/:106/:150/:171/:172/:186/:193/:226）、两个声明块标记（:250/:253）、表头与对齐行（:257/:258）、八条数据行（:259-266）。其余 272 行任删任改不响。
9. **变异 (a)(b)(c)(e)(d) 实测**：全部按声明转红（消息见「验收」节）。(f) 删对齐行、(g) 复制重复编号行、(h) 清空描述列为本轮补测：(f) 红但误诊，(g)(h) **绿**。
10. **上一版对照**：`git show v0.0.128:Tests/…/DebtPayoffTests.swift` 的断言体逐字编译，喂「编号列前插一列」→ `红 → 清单表头必须是以「| 编号 |」开头的一行`；喂「判定挪到编号引用的下一行」→ `绿`。前者推翻注释 ② 的因果，后者证明本轮 ④ 是真加强。
11. `command -v dsh claude codex opencode gemini` → 只有 `dsh` 存在，其余四个未安装。`date` → 2026-09-24 13:54。

**推演（未测量，只作判断）**

- `boldIDs` 的 `**` 切分对**不配对的粗体**会整体错位（段里多一个或少一个 `**`，真编号可能落进「粗体外」的分片、假编号可能落进「粗体内」），本轮没构造出可观察后果，因为两种错位分别由 `orphan`/`noRow` 变红，故未列为问题。
- `tokenID` 用 `Character.isNumber`，全角数字 `V１` 与 `V²` 会被认作合法编号（`isASCII` 只卡在后缀字母上）。当前文档无此写法，未跑变异。
- `relativePath` 的 `hasPrefix(root + "/")` 回退：仓库根路径含符号链接时（枚举器返回的是解引用后的规范路径，与前缀字符串不同源）会静默退化成 `lastPathComponent`，只影响消息可读性，不影响判定。

## 备注

- 审查开始时 `git status` 只有 2 个改动文件（本轮声明范围）；进行到一半工作树又出现 `AppVersion.swift`／`CHANGELOG.md`／`README.md` 三处 0.0.128 → 0.0.129 的同步（`git diff --stat HEAD` 实测 5 文件）。三处版本号已一致为 `0.0.129`，`scripts/release.sh` 的一致性预检（:25-30）可以过。`DebtPayoffTests.swift` 在本轮审查期间被改过一次（+1 行，仅 :458-460 注释），本文所有行号与全部复刻实验均以**改动后**的 526 行版本重跑复核过，结论不变。
- `CHANGELOG.md` 0.0.129 段里「外部 review … 结论：待补／报告见 `docs/code-review/待补`」两处占位需要随本轮一起替换成 `docs/code-review/2026-09-24-1335-v0.0.129-fallback-subagent.md`。
- 「一份文档同时有两张待核实清单表」目前无法表达：实测 `c16-two-tables` 红（第二张表的行不在 `tableRegion`，其状态词与编号被当正文判）。这是设计上限而非缺陷（响），但真需要拆表时只能改断言。
- 本轮无运行时逻辑改动（唯一源码变更是版本号常量），无运行时缺陷面；本报告为只读产物，未修改任何被审文件，`git add/commit/stash/checkout` 一律未执行，全部实验写在 /tmp。
