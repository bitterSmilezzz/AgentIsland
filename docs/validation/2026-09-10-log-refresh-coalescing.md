# 实时流水刷新合并

实时流水页每 2 秒刷新一次。原实现不等待前一轮解析完成，日志较大时会出现并发尾读，旧回调还可能覆盖较新的结果。

现在用在途标志合并请求：手动刷新、首次加载和定时刷新共用同一条读取链路；上一轮完成前的新请求直接跳过，完成后才允许下一轮。结果未变化时也不触发列表动画更新。

验证：核心测试 99/99、`--selftest`、Release 构建和签名校验通过；应用已重启运行。
