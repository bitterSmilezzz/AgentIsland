# R35 验证记录：能效与护栏批（v0.0.52）

日期：2026-09-12 · 执行：主 agent

## 改动
- G2：edgeZoneTimer 自适应降档（活跃 0.12s / 静止 3s → 0.5s；evaluateEdgeZone 返回活跃度驱动切换，热区/展开/拖拽/按压即恢复快档）
- G3：高度签名源级漂移哨兵（六输入 + 渲染实参对齐；变异验证 195/1 红）
- 测试 196/0

## A/B 实测（同条件对照）
过程中 CPU 采样一度显示 5-11%，做了严格对照排除回退：
1. worktree 构建 v0.0.48 基线 app（打包后运行，二进制直跑缺 bundle 会崩——SMAppService 需要 mainBundle.bundleURL）
2. 同条件（WorkBuddy 国内版活跃 69% CPU、anyWorking=true）采样：基线 6.2-7.1%，当前 5.4-8.1%——相当，无回退
3. 高值根因：活跃期高频节律（2s 采样 + 5s 强制全量扫描）+ WorkBuddy 本身工作；收起静态约 0-1%
4. 教训：ps -o %cpu 是生命周期均值（重启后偏差大）、top -l 首样本含启动尖峰——须取稳态多样本并做基线对照

## 运行证据
- 196/0 ×3；selftest 全过；打包重启（0.0.52）
